# Architecture Review Template

When reviewing architecture decisions, check:
1. Does this change respect existing module boundaries?
2. Are new dependencies justified?
3. Is the approach consistent with the project's architectural patterns?
4. Are there simpler alternatives that achieve the same goal?

Return a JSON object with summary, changes, risks, status.
Mark status as "blocked" if any concern is critical.
